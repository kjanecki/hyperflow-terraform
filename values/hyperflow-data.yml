workflow:
  image: hyperflowwms/montage2-workflow-data:montage2-2mass-025-latest

command:
  - "/bin/sh"
  - "-c"
  - >
    echo "Mounting..." ;
    cp -rv /data/* /workflow-data ;
    sed -i 's/exit/noop/g' /workflow-data/workflow.json ;
    echo "Volume mounted" ;